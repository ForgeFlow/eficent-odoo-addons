<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <field name="dest_address_id" position="after">
                <field name="check_analytic_id" invisible="1"/>
            </field>
            <field name="dest_address_id" position="attributes">
                <attribute name="attrs">{
                    'invisible': ['|',('default_location_dest_id_usage', '!=', 'customer'), ('check_analytic_id', '=', True)],
                    'required': [('default_location_dest_id_usage', '=', 'customer'),('check_analytic_id', '=', False)]
                    }
                </attribute>
            </field>
        </field>
    </record>

</odoo>
