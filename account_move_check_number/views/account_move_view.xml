<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record model="ir.ui.view" id="account_move_line_form_view">
        <field name="name">account.move.line.check.form</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_form" />
        <field name="arch" type="xml">
            <field name="move_id" position="after">
                <field name="check_number" />
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="account_move_line_search_view">
        <field name="name">account.move.line.check.search</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_account_move_line_filter" />
        <field name="arch" type="xml">
            <field name="move_id" position="after">
                <field name="check_number" />
            </field>
        </field>
    </record>


</odoo>
